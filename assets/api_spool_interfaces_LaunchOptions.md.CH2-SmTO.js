import{_ as t,o,c as a,a2 as i}from"./chunks/framework.BWuWLRhz.js";const u=JSON.parse('{"title":"Interface: LaunchOptions","description":"","frontmatter":{},"headers":[],"relativePath":"api/spool/interfaces/LaunchOptions.md","filePath":"api/spool/interfaces/LaunchOptions.md"}'),s={name:"api/spool/interfaces/LaunchOptions.md"};function r(n,e,l,c,d,h){return o(),a("div",null,[...e[0]||(e[0]=[i(`<p><a href="./../">spool</a> / LaunchOptions</p><h1 id="interface-launchoptions" tabindex="-1">Interface: LaunchOptions <a class="header-anchor" href="#interface-launchoptions" aria-label="Permalink to &quot;Interface: LaunchOptions&quot;">​</a></h1><p>Defined in: <a href="https://github.com/CodingTwist/compiler/blob/3f0317adfc9e342144a70bee08f595c13cea2c69/spool/src/plugins/ballistics/solve.ts#L85" target="_blank" rel="noreferrer">plugins/ballistics/solve.ts:85</a></p><p><strong>The solver.</strong> Given a launch point, a target point and a speed budget, find the launch velocity - reported as yaw / pitch / speed - whose <em>tick-simulated</em> flight passes through the target.</p><h2 id="why-there-is-no-root-finding-loop" tabindex="-1">Why there is no root-finding loop <a class="header-anchor" href="#why-there-is-no-root-finding-loop" aria-label="Permalink to &quot;Why there is no root-finding loop&quot;">​</a></h2><p>The naive approach is a two-dimensional search: guess a pitch, guess a speed, simulate, measure the miss, iterate. That&#39;s slow <em>and</em> fragile (the miss function is non-monotonic in pitch). It&#39;s also unnecessary, because of the affine structure in <code>physics.ts</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>  p(t) = p₀ + v₀·A(t) + ĵ·G(t)</span></span></code></pre></div><p><code>A</code> and <code>G</code> are fixed once the projectile type is chosen - they don&#39;t depend on the launch at all. So <strong>flight time is the only free parameter</strong>. Pick a time of flight <code>t</code> and the required launch velocity falls out by division, exactly:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>  horizontal:  v_h = R / A(t)                 R = horizontal distance to target</span></span>
<span class="line"><span>  vertical:    v_y = (Δy − G(t)) / A(t)</span></span></code></pre></div><p>Every <code>t</code> for which <code>A(t) &gt; 0</code> yields a launch that hits the target <em>dead on</em>. That turns &quot;solve for speed and angle&quot; into &quot;choose among the one-parameter family of exact solutions&quot;, and the constraints (speed range, pitch range, fuse length, the per-axis <code>Motion</code> limit) become a filter rather than something to search against.</p><p>The scan over <code>t</code> is therefore not a convergence loop - it is enumeration of candidate solutions, each already exact, ranked by <a href="#prefer">LaunchOptions.prefer</a>. A coarser scan can only return a <em>less preferred</em> solution, never a less accurate one. That is the property that lets the default scan be one sample per tick.</p><h2 id="why-the-default-is-whole-ticks" tabindex="-1">Why the default is whole ticks <a class="header-anchor" href="#why-the-default-is-whole-ticks" aria-label="Permalink to &quot;Why the default is whole ticks&quot;">​</a></h2><p>TNT detonates when its <code>fuse</code> counter hits zero, at the <em>end</em> of a tick, at whatever position it holds then - i.e. exactly at a tick sample. Solving on integer ticks makes <code>fuse = t</code> an airburst centred on the target with no rounding error at all. Raise <a href="#subticksamples">LaunchOptions.subTickSamples</a> when the projectile is meant to physically arrive somewhere (an arrow hitting a mob) rather than explode on a timer.</p><h2 id="accuracy-what-you-actually-get-and-what-can-still-differ" tabindex="-1">Accuracy: what you actually get, and what can still differ <a class="header-anchor" href="#accuracy-what-you-actually-get-and-what-can-still-differ" aria-label="Permalink to &quot;Accuracy: what you actually get, and what can still differ&quot;">​</a></h2><p>The returned <code>error</code> is not an estimate. Every solution is re-flown through the real simulate integrator and measured with closestApproach against the swept path, so <code>error</code> is the true closest approach of the modelled flight. It comes back at ~1e-13 blocks (double-precision round-off); anything larger is a real signal that a constraint bent the solution.</p><p>Against that, the ways the <em>game&#39;s</em> TNT can still diverge:</p><ol><li><strong>Blocks.</strong> <code>move()</code> does collision resolution; this solver has no world. A trajectory that clips terrain, a wall, or the ceiling stops there. Check the arc is clear, or use <code>prefer: &quot;min-time&quot;</code> for a flatter, shorter path.</li><li><strong>The ground bounce.</strong> On contact TNT&#39;s velocity becomes <code>(0.7, −0.5, 0.7)×</code> - not modelled, and irrelevant if the shot airbursts before landing.</li><li><strong>Water / lava.</strong> Buoyancy and fluid drag replace the air model entirely.</li><li><strong>Spawn timing.</strong> An entity summoned mid-tick is queued and first ticks on the <em>following</em> tick, so flight time is counted from the tick after the <code>/summon</code>. If the launcher runs in a chain of functions this is consistent; if you compare against a manually-placed TNT, expect a one-tick offset.</li><li><strong>The explosion is offset.</strong> <code>PrimedTnt</code> explodes at <code>getY(0.0625)</code>, i.e. <strong>6.1 cm above</strong> the entity&#39;s feet position, and blast damage is computed from there.</li><li><strong>Where the entity sits.</strong> <code>/summon</code> places the entity&#39;s feet at the coordinate you give; the arc is solved for that same feet point, so aim at your target&#39;s feet too (or add the eye height yourself).</li><li><strong>Chunk loading.</strong> An unloaded destination means nothing ticks there. Forceload the corridor for long shots.</li><li><strong>Other projectiles.</strong> Arrows fired by an entity get random spread applied to the launch vector; only a directly-<code>Motion</code>-set arrow follows this model.</li></ol><h2 id="extended-by" tabindex="-1">Extended by <a class="header-anchor" href="#extended-by" aria-label="Permalink to &quot;Extended by&quot;">​</a></h2><ul><li><a href="./BallisticOptions.html"><code>BallisticOptions</code></a></li></ul><h2 id="properties" tabindex="-1">Properties <a class="header-anchor" href="#properties" aria-label="Permalink to &quot;Properties&quot;">​</a></h2><h3 id="maxspeed" tabindex="-1">maxSpeed? <a class="header-anchor" href="#maxspeed" aria-label="Permalink to &quot;maxSpeed?&quot;">​</a></h3><blockquote><p><code>readonly</code> <code>optional</code> <strong>maxSpeed?</strong>: <code>number</code></p></blockquote><p>Defined in: <a href="https://github.com/CodingTwist/compiler/blob/3f0317adfc9e342144a70bee08f595c13cea2c69/spool/src/plugins/ballistics/solve.ts#L94" target="_blank" rel="noreferrer">plugins/ballistics/solve.ts:94</a></p><p>Reject solutions faster than this, in blocks/tick. Default <code>MOTION_AXIS_LIMIT</code> (10) - the point past which vanilla zeroes the <code>Motion</code> tag. A per-axis check applies too.</p><hr><h3 id="maxticks" tabindex="-1">maxTicks? <a class="header-anchor" href="#maxticks" aria-label="Permalink to &quot;maxTicks?&quot;">​</a></h3><blockquote><p><code>readonly</code> <code>optional</code> <strong>maxTicks?</strong>: <code>number</code></p></blockquote><p>Defined in: <a href="https://github.com/CodingTwist/compiler/blob/3f0317adfc9e342144a70bee08f595c13cea2c69/spool/src/plugins/ballistics/solve.ts#L101" target="_blank" rel="noreferrer">plugins/ballistics/solve.ts:101</a></p><p>Latest allowed flight time. Defaults to the projectile&#39;s fuse (80 for TNT) - a longer flight would detonate in the air on the way there.</p><hr><h3 id="minspeed" tabindex="-1">minSpeed? <a class="header-anchor" href="#minspeed" aria-label="Permalink to &quot;minSpeed?&quot;">​</a></h3><blockquote><p><code>readonly</code> <code>optional</code> <strong>minSpeed?</strong>: <code>number</code></p></blockquote><p>Defined in: <a href="https://github.com/CodingTwist/compiler/blob/3f0317adfc9e342144a70bee08f595c13cea2c69/spool/src/plugins/ballistics/solve.ts#L89" target="_blank" rel="noreferrer">plugins/ballistics/solve.ts:89</a></p><p>Reject solutions slower than this, in blocks/tick. Default <code>0</code>.</p><hr><h3 id="minticks" tabindex="-1">minTicks? <a class="header-anchor" href="#minticks" aria-label="Permalink to &quot;minTicks?&quot;">​</a></h3><blockquote><p><code>readonly</code> <code>optional</code> <strong>minTicks?</strong>: <code>number</code></p></blockquote><p>Defined in: <a href="https://github.com/CodingTwist/compiler/blob/3f0317adfc9e342144a70bee08f595c13cea2c69/spool/src/plugins/ballistics/solve.ts#L96" target="_blank" rel="noreferrer">plugins/ballistics/solve.ts:96</a></p><p>Earliest allowed flight time, in ticks. Default <code>1</code>.</p><hr><h3 id="pitchrange" tabindex="-1">pitchRange? <a class="header-anchor" href="#pitchrange" aria-label="Permalink to &quot;pitchRange?&quot;">​</a></h3><blockquote><p><code>readonly</code> <code>optional</code> <strong>pitchRange?</strong>: readonly [<code>number</code>, <code>number</code>]</p></blockquote><p>Defined in: <a href="https://github.com/CodingTwist/compiler/blob/3f0317adfc9e342144a70bee08f595c13cea2c69/spool/src/plugins/ballistics/solve.ts#L106" target="_blank" rel="noreferrer">plugins/ballistics/solve.ts:106</a></p><p>Restrict the launch angle, <code>[min, max]</code> in degrees, <strong>negative is up</strong>. <code>[-90, -45]</code> forces a mortar-style lob; <code>[-20, 0]</code> a flat direct-fire shot.</p><hr><h3 id="prefer" tabindex="-1">prefer? <a class="header-anchor" href="#prefer" aria-label="Permalink to &quot;prefer?&quot;">​</a></h3><blockquote><p><code>readonly</code> <code>optional</code> <strong>prefer?</strong>: <code>&quot;min-speed&quot;</code> | <code>&quot;min-time&quot;</code> | <code>&quot;max-time&quot;</code></p></blockquote><p>Defined in: <a href="https://github.com/CodingTwist/compiler/blob/3f0317adfc9e342144a70bee08f595c13cea2c69/spool/src/plugins/ballistics/solve.ts#L115" target="_blank" rel="noreferrer">plugins/ballistics/solve.ts:115</a></p><p>Which of the exact solutions to return:</p><ul><li><code>&quot;min-speed&quot;</code> (default) - the least energetic shot that reaches, the classic artillery solution and the most forgiving of a speed cap.</li><li><code>&quot;min-time&quot;</code> - flattest and fastest to arrive; least time for a target to move, but the highest speed and the lowest clearance over terrain.</li><li><code>&quot;max-time&quot;</code> - the highest lob that still fits the budget; clears walls.</li></ul><hr><h3 id="projectile" tabindex="-1">projectile? <a class="header-anchor" href="#projectile" aria-label="Permalink to &quot;projectile?&quot;">​</a></h3><blockquote><p><code>readonly</code> <code>optional</code> <strong>projectile?</strong>: <a href="./ProjectileProfile.html"><code>ProjectileProfile</code></a></p></blockquote><p>Defined in: <a href="https://github.com/CodingTwist/compiler/blob/3f0317adfc9e342144a70bee08f595c13cea2c69/spool/src/plugins/ballistics/solve.ts#L87" target="_blank" rel="noreferrer">plugins/ballistics/solve.ts:87</a></p><p>Which entity to model. Default PROJECTILES.tnt.</p><hr><h3 id="subticksamples" tabindex="-1">subTickSamples? <a class="header-anchor" href="#subticksamples" aria-label="Permalink to &quot;subTickSamples?&quot;">​</a></h3><blockquote><p><code>readonly</code> <code>optional</code> <strong>subTickSamples?</strong>: <code>number</code></p></blockquote><p>Defined in: <a href="https://github.com/CodingTwist/compiler/blob/3f0317adfc9e342144a70bee08f595c13cea2c69/spool/src/plugins/ballistics/solve.ts#L120" target="_blank" rel="noreferrer">plugins/ballistics/solve.ts:120</a></p><p>Candidate flight times sampled per tick. Default <code>1</code> (whole ticks only - see the fuse note above). Raise for a projectile that must physically arrive between ticks.</p>`,60)])])}const f=t(s,[["render",r]]);export{u as __pageData,f as default};
