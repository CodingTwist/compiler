// dataVersion of the snapshot each advancement format change landed in.

/**
 * 26.3 Snapshot 4: `player`/`entity` take one predicate instead of an entity predicate or
 * condition list, and `enter_block`'s `block` became `blocks`.
 */
export const PREDICATE_FIELDS_DATA_VERSION = 5003;
