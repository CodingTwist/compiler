import{_ as t,o,c as a,a2 as c}from"./chunks/framework.BWuWLRhz.js";const u=JSON.parse('{"title":"Function: consolidateTick()","description":"","frontmatter":{},"headers":[],"relativePath":"api/twine/functions/consolidateTick.md","filePath":"api/twine/functions/consolidateTick.md"}'),n={name:"api/twine/functions/consolidateTick.md"};function i(r,e,d,s,l,p){return o(),a("div",null,[...e[0]||(e[0]=[c('<p><a href="./../">twine</a> / consolidateTick</p><h1 id="function-consolidatetick" tabindex="-1">Function: consolidateTick() <a class="header-anchor" href="#function-consolidatetick" aria-label="Permalink to &quot;Function: consolidateTick()&quot;">​</a></h1><blockquote><p><strong>consolidateTick</strong>(<code>dp</code>): <code>void</code></p></blockquote><p>Defined in: <a href="https://github.com/CodingTwist/compiler/blob/4c7aecb01ce1911b4091ca195d44fb415791a426/twine/src/factory.ts#L218" target="_blank" rel="noreferrer">factory.ts:218</a></p><p>Collapse <code>minecraft:tick</code> to the single framework-owned <code>&lt;ns&gt;:tick</code> entry.</p><p>helix auto-tags every function created with the <code>tick</code> tag straight into vanilla <code>minecraft:tick</code> - spool plugins (<code>grapple/tick</code>), <code>defineItem</code> item ticks, the scoreboard clock, etc. That&#39;s the right un-opinionated default for a plain-helix pack, but under the framework the per-tick surface should be one thing you own: a single tag member whose body lists every per-tick function, so the whole pack&#39;s tick cost is traceable in one place and gateable as a unit.</p><p>So reparent: untag every <em>other</em> <code>tick</code> member from <code>minecraft:tick</code> and append a <code>function</code> call to it onto the root <code>tick</code> body (after the module tree-walk). Same work runs each tick, now dispatched from - and visible in - <code>&lt;ns&gt;:tick</code>.</p><p><code>DatapackFactory.create</code> runs this once over the module tree. It&#39;s also <strong>idempotent</strong> and exported, so a consumer that adds more <code>tick</code>-tagged functions imperatively <em>after</em> <code>create</code> (raw helix/spool calls) can re-run it just before <code>writeDatapack</code> to sweep those too - already-reparented members are no longer in the tag, so a re-run only collapses the new ones.</p><h2 id="parameters" tabindex="-1">Parameters <a class="header-anchor" href="#parameters" aria-label="Permalink to &quot;Parameters&quot;">​</a></h2><h3 id="dp" tabindex="-1">dp <a class="header-anchor" href="#dp" aria-label="Permalink to &quot;dp&quot;">​</a></h3><p><code>Datapack</code></p><h2 id="returns" tabindex="-1">Returns <a class="header-anchor" href="#returns" aria-label="Permalink to &quot;Returns&quot;">​</a></h2><p><code>void</code></p>',13)])])}const k=t(n,[["render",i]]);export{u as __pageData,k as default};
