import{_ as t,o,c as a,a2 as r}from"./chunks/framework.BWuWLRhz.js";const g=JSON.parse('{"title":"Interface: ProjectileProfile","description":"","frontmatter":{},"headers":[],"relativePath":"api/spool/interfaces/ProjectileProfile.md","filePath":"api/spool/interfaces/ProjectileProfile.md"}'),s={name:"api/spool/interfaces/ProjectileProfile.md"};function i(c,e,l,n,d,p){return o(),a("div",null,[...e[0]||(e[0]=[r(`<p><a href="./../">spool</a> / ProjectileProfile</p><h1 id="interface-projectileprofile" tabindex="-1">Interface: ProjectileProfile <a class="header-anchor" href="#interface-projectileprofile" aria-label="Permalink to &quot;Interface: ProjectileProfile&quot;">​</a></h1><p>Defined in: <a href="https://github.com/CodingTwist/compiler/blob/4c7aecb01ce1911b4091ca195d44fb415791a426/spool/src/plugins/ballistics/physics.ts#L55" target="_blank" rel="noreferrer">plugins/ballistics/physics.ts:55</a></p><p><strong>The physics half of <code>ballistics</code>: Minecraft&#39;s real per-tick projectile integrator.</strong></p><p>Nothing here is an idealised parabola. Every number below is transcribed from the vanilla entity classes, and stepOnce performs the operations in the order the game performs them - because that order is <em>observable</em>, not a detail:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>PrimedTnt.tick():                         AbstractArrow / ThrowableProjectile.tick():</span></span>
<span class="line"><span>  if (!isNoGravity()) applyGravity();       move(SELF, deltaMovement);</span></span>
<span class="line"><span>  move(SELF, deltaMovement);                setDeltaMovement(delta.scale(inertia));</span></span>
<span class="line"><span>  setDeltaMovement(delta.scale(0.98));      if (!isNoGravity())</span></span>
<span class="line"><span>  if (onGround()) delta.multiply(.7,-.5,.7)   setDeltaMovement(delta.add(0,-gravity,0));</span></span></code></pre></div><p>So <strong>TNT applies gravity <em>before</em> it moves</strong> (the very first tick of flight already carries a full gravity step) and <strong>drag <em>after</em></strong> (so the first tick&#39;s displacement is the raw, undragged launch velocity). Arrows and throwables do the opposite - move, drag, <em>then</em> gravity - which is why the same launch vector produces a different curve for a snowball than for TNT. Getting that ordering backwards is a systematic ~1 tick of gravity, roughly half a block over a long shot.</p><p>Two further facts that shape the whole solver:</p><ul><li><strong>Drag is isotropic.</strong> <code>delta.scale(0.98)</code> multiplies <em>all three</em> axes, y included. Vertical velocity is dragged exactly like horizontal velocity, which is why TNT reaches a terminal state rather than falling forever: the stored <code>deltaMovement.y</code> converges on <code>-g·d/(1-d)</code> = <code>-1.96</code>, and since gravity is applied <em>before</em> the move, the observed fall rate settles at exactly <code>-2.0</code> blocks per tick.</li><li><strong>Gravity is a constant additive step</strong>, independent of velocity.</li></ul><p>Together those two make the tick map <strong>affine in the launch velocity</strong> - the property trajectoryBasis exploits to invert the trajectory exactly. See <code>solve.ts</code>.</p><h2 id="coordinate-system-and-angle-conventions" tabindex="-1">Coordinate system and angle conventions <a class="header-anchor" href="#coordinate-system-and-angle-conventions" aria-label="Permalink to &quot;Coordinate system and angle conventions&quot;">​</a></h2><p>Minecraft world axes: <strong>+X east, +Y up, +Z south</strong>. Rotation is stored as <code>[yaw, pitch]</code> in degrees:</p><ul><li><strong>yaw</strong> <code>0</code> faces <strong>+Z (south)</strong> and increases <strong>clockwise seen from above</strong>, so <code>90</code> faces <strong>-X (west)</strong>, <code>180</code> faces <code>-Z</code> (north), <code>-90</code>/<code>270</code> faces <code>+X</code> (east).</li><li><strong>pitch</strong> <code>0</code> is horizontal, <strong>negative is up</strong>, positive is down (<code>-90</code> = straight up).</li></ul><p>The unit look vector for a rotation is therefore <code>(-sin y · cos p, -sin p, cos y · cos p)</code>, and the inverse (used by the solver) is <code>yaw = atan2(-vx, vz)</code>, <code>pitch = atan2(-vy, hypot(vx, vz))</code>.</p><p><strong>TNT ignores rotation entirely</strong> - it is launched by writing the <code>Motion</code> tag, a raw velocity vector. The yaw/pitch a solution reports are the spherical decomposition of that vector: useful to aim a display entity, a particle, or a bow-like projectile, and exactly what you would feed <code>/summon</code> for an entity that <em>does</em> read rotation.</p><h2 id="properties" tabindex="-1">Properties <a class="header-anchor" href="#properties" aria-label="Permalink to &quot;Properties&quot;">​</a></h2><h3 id="defaultfuse" tabindex="-1">defaultFuse? <a class="header-anchor" href="#defaultfuse" aria-label="Permalink to &quot;defaultFuse?&quot;">​</a></h3><blockquote><p><code>readonly</code> <code>optional</code> <strong>defaultFuse?</strong>: <code>number</code></p></blockquote><p>Defined in: <a href="https://github.com/CodingTwist/compiler/blob/4c7aecb01ce1911b4091ca195d44fb415791a426/spool/src/plugins/ballistics/physics.ts#L68" target="_blank" rel="noreferrer">plugins/ballistics/physics.ts:68</a></p><p><code>fuse</code> ticks a <code>/summon</code>ed one starts with, where the entity has a fuse at all.</p><hr><h3 id="drag" tabindex="-1">drag <a class="header-anchor" href="#drag" aria-label="Permalink to &quot;drag&quot;">​</a></h3><blockquote><p><code>readonly</code> <strong>drag</strong>: <code>number</code></p></blockquote><p>Defined in: <a href="https://github.com/CodingTwist/compiler/blob/4c7aecb01ce1911b4091ca195d44fb415791a426/spool/src/plugins/ballistics/physics.ts#L61" target="_blank" rel="noreferrer">plugins/ballistics/physics.ts:61</a></p><p>Per-tick velocity multiplier applied to <strong>all three axes</strong> (vanilla calls it inertia).</p><hr><h3 id="gravity" tabindex="-1">gravity <a class="header-anchor" href="#gravity" aria-label="Permalink to &quot;gravity&quot;">​</a></h3><blockquote><p><code>readonly</code> <strong>gravity</strong>: <code>number</code></p></blockquote><p>Defined in: <a href="https://github.com/CodingTwist/compiler/blob/4c7aecb01ce1911b4091ca195d44fb415791a426/spool/src/plugins/ballistics/physics.ts#L59" target="_blank" rel="noreferrer">plugins/ballistics/physics.ts:59</a></p><p>Blocks/tick² subtracted from <code>vy</code> once per tick (<code>Entity.getDefaultGravity()</code>).</p><hr><h3 id="gravitybeforemove" tabindex="-1">gravityBeforeMove <a class="header-anchor" href="#gravitybeforemove" aria-label="Permalink to &quot;gravityBeforeMove&quot;">​</a></h3><blockquote><p><code>readonly</code> <strong>gravityBeforeMove</strong>: <code>boolean</code></p></blockquote><p>Defined in: <a href="https://github.com/CodingTwist/compiler/blob/4c7aecb01ce1911b4091ca195d44fb415791a426/spool/src/plugins/ballistics/physics.ts#L66" target="_blank" rel="noreferrer">plugins/ballistics/physics.ts:66</a></p><p><code>true</code> for the TNT/falling-block family (gravity → move → drag), <code>false</code> for the arrow/throwable family (move → drag → gravity). Load-bearing; see the file docstring.</p><hr><h3 id="id" tabindex="-1">id <a class="header-anchor" href="#id" aria-label="Permalink to &quot;id&quot;">​</a></h3><blockquote><p><code>readonly</code> <strong>id</strong>: <code>string</code></p></blockquote><p>Defined in: <a href="https://github.com/CodingTwist/compiler/blob/4c7aecb01ce1911b4091ca195d44fb415791a426/spool/src/plugins/ballistics/physics.ts#L57" target="_blank" rel="noreferrer">plugins/ballistics/physics.ts:57</a></p><p>The entity id to <code>/summon</code>.</p>`,40)])])}const f=t(s,[["render",i]]);export{g as __pageData,f as default};
